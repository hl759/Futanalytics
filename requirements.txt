# MESMAS quatro dependências do FutAnalytics — a migração não adicionou nenhuma.
# Restrição do Render free tier (512MB RAM) e decisão de projeto: toda a
# matemática (EWMA, Parkinson/Garman-Klass/Yang-Zhang, bipower variation,
# Ornstein-Uhlenbeck, fórmula VIX model-free, regressão PAVA) é Python puro da
# stdlib. Sem numpy, sem pandas, sem scipy, sem sklearn.
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
pydantic>=2.6
